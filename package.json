{
  "name": "claude-widget",
  "version": "0.1.0",
  "private": true,
  "description": "claudget: your Claude Code usage limits in the menu bar.",
  "license": "MIT",
  "workspaces": [
    "packages/*"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build:core": "node scripts/run-workspace.mjs core build",
    "build": "node scripts/run-workspace.mjs core build && node scripts/run-workspace.mjs desktop build",
    "dev": "node scripts/run-workspace.mjs core build && node scripts/run-workspace.mjs desktop dev",
    "start": "node scripts/run-workspace.mjs desktop start",
    "dev:core": "node scripts/run-workspace.mjs core dev",
    "package": "node scripts/run-workspace.mjs core build && node scripts/run-workspace.mjs desktop package",
    "package:dir": "node scripts/run-workspace.mjs core build && node scripts/run-workspace.mjs desktop package:dir",
    "typecheck": "node scripts/run-workspace.mjs core typecheck && node scripts/run-workspace.mjs desktop typecheck",
    "test": "node scripts/run-workspace.mjs core test && node scripts/run-workspace.mjs desktop test",
    "lint": "eslint .",
    "format": "prettier --write \"**/*.{ts,tsx,js,mjs,json,css,md}\"",
    "format:check": "prettier --check \"**/*.{ts,tsx,js,mjs,json,css,md}\""
  },
  "devDependencies": {
    "@eslint/js": "^9.39.5",
    "@types/node": "^22.20.4",
    "eslint": "^9.39.5",
    "eslint-config-prettier": "^9.1.2",
    "prettier": "^3.9.9",
    "shadcn": "^4.21.0",
    "typescript": "^5.9.3",
    "typescript-eslint": "^8.70.1"
  }
}
