# Dependencies
node_modules/
.npm/

# npm's package-lock.json is the committed lockfile; other managers generate local locks.
bun.lock
pnpm-lock.yaml
yarn.lock

# Build output
dist/
out/
build/release/
packages/desktop/release/
*.tsbuildinfo

# Logs
logs/
*.log
npm-debug.log*

# Editor / OS
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json

# Test coverage
coverage/

# Local env / secrets (never commit credentials)
.env
.env.local
*.local.json

# Local working notes & tooling — not part of the open-source project
.claude/
.vercel/
.mcp.json
CLAUDE.md
DESIGN.md
POSTMORTEM.md
context.md
test-electron-api.cjs

# Launch-video output (brag-slim): renders, frames, stems.
brag-output/
